import { Meta, StoryObj } from '@storybook/react'
import { SignIn } from "./SignIn";

export default {
  title: 'Pages/SignIn',
  component: SignIn,
  args: {}
} as Meta

export const Default: StoryObj = {}
